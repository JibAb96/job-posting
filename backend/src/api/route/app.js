import express from "express";

const router = express.Router();

router.get("/", 
    //getPosts
    );
router.post("/create",
    //createPost
);
router.put("/edit",
    //editPost
);
router.delete("/", 
    //deletePost
);
